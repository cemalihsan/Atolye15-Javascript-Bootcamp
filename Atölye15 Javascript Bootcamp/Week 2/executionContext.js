//GLOBAL EXECUTION CONTEXT

//At beginning JS has window and this as global object in an execution of an empty JS page

//Creation Phase:
// Variables are defined at first stage as undefined and function as fn()

//Execution Phase:
//Variable Decleration(Variable Assignment) is done in this stage(assigning the values of each variable) 

//HOISTING: Creation fazında variable'ların undefined olarak tanımlanması(In creation step, defines all variables as undefined)


//FUNCTION EXECUTION CONTEXT

//Won't create a window object, instead creates arguments object(that gave to the function as arguments)

//Creation Phase:
//Assigns variable declerations as undefined inside the scope first

